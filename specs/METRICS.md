# METRICS —— 执行度量

> 每个任务一行。人工介入 = 流程被迫停下来问人的次数（入口授权门不计）。
> 审查列标 `降级` = 本次运行 Codex 未安装，代码审查走对抗式子代理（质量次优）。

## 1.ui-shell-redesign

| 任务 | 状态 | 审查 | 审查拦截 | 人工介入 | 备注 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| T-001 建立防护网基线 | ✅ 完成 | 对抗子代理 `降级` | **5 条**（2 严重 · 全部采纳） | 0 | 92 测试 / 5 变异全抓 |
| T-001b 修静默改数据缺陷 | ✅ 完成 | 自审 | — | 0 | 计划外，见决策记录 |
| T-002 拆出 util.js 与 tree.js | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 严重 · 全部采纳） | 0 | core.js 373→266 行 |
| T-003 拆出 toolbar/search/rail/status | ⬜ | | | | |
| T-003b 实现 tree.jumpTo | ⬜ | | | | |
| T-004 工具栏 IA | ⬜ | | | | |
| T-005 重构 viewer.css | ⬜ | | | | |
| T-006 重做主视图视觉 | ⬜ | | | | |
| T-007 重做 popup + viewer 页 | ⬜ | | | | |
| T-008 popup 三态 | ⬜ | | | | |
| T-009 空态与无结果态 | ⬜ | | | | |
| T-010 大文件加载态 | ⬜ | | | | |
| T-011 元素清单核对 | ⬜ | | | | |
| T-012 全量冒烟 + 复跑基线 | ⬜ | | | | |

### T-001 详情

- **产出**：`tests/` 5 个文件 92 条测试（此前项目零自动化测试）
  - `_load.mjs` 加载器（IIFE → Node，不改造被测代码）
  - `_dom.mjs` 最小 DOM 桩（不引 jsdom，守零依赖）
  - `jsonbig.test.mjs` 解析层 / `normalize.test.mjs` 容错 / `esc.test.mjs` 转义函数 / `tree.test.mjs` **转义调用点** / `loader.test.mjs` **加载拓扑**
- **对抗审查拦截 5 条，2 条严重且已实测复现**：
  1. XSS 防线只测函数不测调用点 → `esc(key)` 改裸 `key` 时 58 条全绿（真 XSS 漏网）
  2. 加载拓扑零覆盖 → manifest 漏登记时 58 条全绿（接管 100% 失效漏网）
  3. 正则抠源码手法会让行为保持的重构误报红
  4. `[现状缺陷]` 断言过松（任何错误都满足）
  5. T-002/T-003 要搬的 5 个纯函数零覆盖
- **修复后**：58 → 92 条；5 个变异全部被抓（改调用点/改 manifest/改 viewer.html，**不改被测函数定义**）
- **自我纠错**：第一轮变异测试无效 —— 改的是 `escAttr` 定义，而测试正好读那两行，测的和改的是同一个东西
- **顺带修掉**：`.claude/rules/testing.md` 里我自己写错的 `node --test tests/`（Node 24 报 MODULE_NOT_FOUND）

## 待裁决（阻塞相应 feature，不阻塞 T-002）

| # | 事项 | 当前决定 | 状态 |
| --- | --- | --- | --- |
| 1 | `[1e]`→NaN→Copy 出 `null`，v0.8.0 线上静默改数据 | **已修**（T-001b，用户授权自主决策） | ✅ 已闭环 |
| 2 | Table 第 1001 条无 AC 覆盖，vlist 装不下表格 | 到 feature 2 再定 | 待人确认 |
| 3 | 50MB 首屏承诺已删，是否单开性能 feature | 不单开 | 待人确认 |

### T-002 详情

- **产出**：`util.js`(50) + `tree.js`(165) 新增；`core.js` 373 → 266 行（400 行上限松开，这是后 4 个 feature 的前置）
- **对抗审查拦截 4 条**（搬运本身经差分测试验证零差异 —— 12 组输入的 HTML/counts/nodes/topLevel 完全一致）：
  1. **严重**：拆分让 `content.js` 的 `!window.JK` 守卫失效 → manifest 漏登记 tree.js 时**会清空用户页面**（已实证）。修法：`mountViewer` 入口检查依赖 + 守卫查 `mountViewer` 类型
  2. 新增 API 面零覆盖（L-001 重演）—— 6 个变异 5 个存活，根因是 DOM 桩 `querySelector` 不记忆化导致 `expandTo` 空转
  3. 面包屑双重注册 —— 提前接 `onCrumb` 导致点复制按钮会改面包屑（原来不会）。已撤回，交还 T-003
  4. `util.js` 注释谎称 popup 能用 normalize（popup 没加载它）。已改成实话
- **修复后**：102 → 113 测试；此前存活的变异 4/5 被抓；第 5 个查证为**死代码**（`byPath` 的 first-wins 守卫空转），已删除而非补测试
