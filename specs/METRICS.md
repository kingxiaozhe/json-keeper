# METRICS —— 执行度量

> 每个任务一行。人工介入 = 流程被迫停下来问人的次数（入口授权门不计）。
> 审查列标 `降级` = 本次运行 Codex 未安装，代码审查走对抗式子代理（质量次优）。

## 1.ui-shell-redesign

| 任务 | 状态 | 审查 | 审查拦截 | 人工介入 | 备注 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| T-001 建立防护网基线 | ✅ 完成 | 对抗子代理 `降级` | **5 条**（2 严重 · 全部采纳） | 0 | 92 测试 / 5 变异全抓 |
| T-001b 修静默改数据缺陷 | ✅ 完成 | 自审 | — | 0 | 计划外，见决策记录 |
| T-002 拆出 util.js 与 tree.js | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 严重 · 全部采纳） | 0 | core.js 373→266 行 |
| T-003 拆出 toolbar/search/rail/status | ✅ 完成 | 对抗子代理 `降级` | **5 条**（1 严重 · 全部采纳） | 0 | core.js 266→181 行 |
| T-003b 实现 tree.jumpTo | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 严重 · 全部采纳） | 0 | 17 靶 10 存活 → 修后仍有 8 靶靠冒烟兜底 |
| T-004 工具栏 IA | ✅ 完成 | 对抗子代理 `降级` | **6 条**（2 回归 · 全部采纳） | 0 | 换肤靶 0/7 → 4/4 |
| T-005 重构 viewer.css | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 严重 · 全部采纳） | 0 | 13 靶 5 存活 → 修后 4/4 |
| T-006 重做主视图视觉 | ⬜ | | | | |
| T-007 重做 popup + viewer 页 | ✅ 完成 | 对抗子代理 `降级` | **3 条**（1 回归 · 全部采纳） | 0 | 发布包 74→21 文件 |
| T-008 popup 三态 | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 回归 · 全部采纳） | 0 | 13 靶 9 抓 |
| T-009 空态与无结果态 | ✅ 完成 | 对抗子代理 `降级` | **4 条**（1 缺功能 · 全部采纳） | 0 | 11 靶 8 抓 → 修后 10/11（1 靶桩够不到） |
| T-010 大文件加载态 | ⬜ | | | | |
| T-011 元素清单核对 | ⬜ | | | | |
| T-012 全量冒烟 + 复跑基线 | ⬜ | | | | |

### T-001 详情

- **产出**：`tests/` 5 个文件 92 条测试（此前项目零自动化测试）
  - `_load.mjs` 加载器（IIFE → Node，不改造被测代码）
  - `_dom.mjs` 最小 DOM 桩（不引 jsdom，守零依赖）
  - `jsonbig.test.mjs` 解析层 / `normalize.test.mjs` 容错 / `esc.test.mjs` 转义函数 / `tree.test.mjs` **转义调用点** / `loader.test.mjs` **加载拓扑**
- **对抗审查拦截 5 条，2 条严重且已实测复现**：
  1. XSS 防线只测函数不测调用点 → `esc(key)` 改裸 `key` 时 58 条全绿（真 XSS 漏网）
  2. 加载拓扑零覆盖 → manifest 漏登记时 58 条全绿（接管 100% 失效漏网）
  3. 正则抠源码手法会让行为保持的重构误报红
  4. `[现状缺陷]` 断言过松（任何错误都满足）
  5. T-002/T-003 要搬的 5 个纯函数零覆盖
- **修复后**：58 → 92 条；5 个变异全部被抓（改调用点/改 manifest/改 viewer.html，**不改被测函数定义**）
- **自我纠错**：第一轮变异测试无效 —— 改的是 `escAttr` 定义，而测试正好读那两行，测的和改的是同一个东西
- **顺带修掉**：`.claude/rules/testing.md` 里我自己写错的 `node --test tests/`（Node 24 报 MODULE_NOT_FOUND）

## 待裁决（阻塞相应 feature，不阻塞 T-002）

| # | 事项 | 当前决定 | 状态 |
| --- | --- | --- | --- |
| 1 | `[1e]`→NaN→Copy 出 `null`，v0.8.0 线上静默改数据 | **已修**（T-001b，用户授权自主决策） | ✅ 已闭环 |
| 2 | Table 第 1001 条无 AC 覆盖，vlist 装不下表格 | 到 feature 2 再定 | 待人确认 |
| 3 | 50MB 首屏承诺已删，是否单开性能 feature | 不单开 | 待人确认 |

### T-002 详情

- **产出**：`util.js`(50) + `tree.js`(165) 新增；`core.js` 373 → 266 行（400 行上限松开，这是后 4 个 feature 的前置）
- **对抗审查拦截 4 条**（搬运本身经差分测试验证零差异 —— 12 组输入的 HTML/counts/nodes/topLevel 完全一致）：
  1. **严重**：拆分让 `content.js` 的 `!window.JK` 守卫失效 → manifest 漏登记 tree.js 时**会清空用户页面**（已实证）。修法：`mountViewer` 入口检查依赖 + 守卫查 `mountViewer` 类型
  2. 新增 API 面零覆盖（L-001 重演）—— 6 个变异 5 个存活，根因是 DOM 桩 `querySelector` 不记忆化导致 `expandTo` 空转
  3. 面包屑双重注册 —— 提前接 `onCrumb` 导致点复制按钮会改面包屑（原来不会）。已撤回，交还 T-003
  4. `util.js` 注释谎称 popup 能用 normalize（popup 没加载它）。已改成实话
- **修复后**：102 → 113 测试；此前存活的变异 4/5 被抓；第 5 个查证为**死代码**（`byPath` 的 first-wins 守卫空转），已删除而非补测试

### T-003 详情

- **产出**：`toolbar.js`(96) + `search.js`(73) + `rail.js`(51) + `status.js`(47)；`core.js` 266 → 181 行，收敛为编排层。全部模块 ≤400 行
- **拆分本身零回归**（对抗审查逐项验证 5 个等价性问题，全部等价），且**顺带修好 2 个存量 bug**：
  - 折叠后点 Sort 需点两次才能再折叠（`resetFold`）
  - rail 每次 renderTree 重挂 scroll 监听、旧监听持 stale topLevel（泄漏）
- **审查拦截 5 条**：
  1. **严重**：防护网不成立 —— 12 个变异 10 个存活，`applySort` 在测试里**一次都没执行过**（`store.get` 走 catch → 恢复分支不可达）
  2. Sort 后 `matches` 悬空 → 计数器报「1/1」、高亮在 detached 行、页面弹顶。**而 search.js 的注释声称这已修好** —— L-006「写下诊断≠修好问题」重演
  3. search 的 `expandAll` 让折叠按钮失步 → 要点两次。`resetFold` 是解药但只装了一半
  4. search.js 关于 storage 的注释是假的（`ensurePretty` 确实会写 storage）
  5. `SHELL.replace` 的 `$` 陷阱 —— 今天不炸，但 T-004 就要重写 BAR_HTML
- **修复后的真实战果**：10 个变异 **4 个**被自动化抓（不是"全抓"）。剩 6 个**结构性测不到**（桩不解析 innerHTML → 工具栏按钮是幻影）→ **逐条写进冒烟清单第 11–15 项**，并在 `mount.test.mjs` 头部写明这层没有网
- **决定：不引 jsdom** —— 零依赖/无远程代码是产品立身之本，testing.md 本就把 DOM 层划给手动冒烟
- 测试 141 → 149

### T-003b 详情

- **产出**：`tree.jumpTo(apath, {align})` 五步齐全；`topLevel` 带 apath；rail 改走 jumpTo（一份实现）
- **挖出并修掉 2 个存量 bug**：
  1. **Raw 下点结构栏无反应** —— `setView("raw")` 不隐藏 `railEl`，rail 去滚一个 hidden 的 prettyEl
  2. **折叠块漏内容** —— `_collapse` 的 `blockRows` 是全部后代，展开外层会把内层仍折叠的块一起露出来（caret 显示折叠、内容却露着）。不用 jumpTo 就能复现，且 160 条测试从未红过
- **审查拦截 4 条**（独立选靶 17 个，**10 个存活**）：
  1. **严重**：rail→jumpToPath→tree 整条装配链 8 个变异全绿。最狠：core 漏传 `jumpTo` → 结构栏抛异常彻底死掉 → 160 条全绿
  2. 我新增的 mount 测试是**安慰剂**，且断言与生产**相反**（详见 LESSONS L-011）
  3. 祖先判定 `<` 改 `<=` 会误展开兄弟块 → 全绿（缺反向断言）
  4. **点结构栏静默改写用户跨会话的视图偏好**（`setView` 无条件持久化）
- **修复后**：反向断言已抓到 `<=` 变异；`setView(v, persist)` 让导航不写偏好；安慰剂测试**删除**；8 个结构性测不到的路径 → 冒烟清单第 16–19 项
- 测试 157 → 160（净减 1 条安慰剂 + 加反向断言与折叠泄漏断言）

### T-004 详情

- **产出**：三段式工具栏 + `⋯` 溢出菜单 + `addMenuItem` 契约（后 4 个 feature 的唯一挂载入口）
- **一个分歧按证据定**：design 草图写 `Tree|Table|Raw|Min`，但设计稿实物是 `Raw|Pretty|Table|Min`（`Tree` 是它左栏导航条目，feature 4 已明确不采用那套左栏），README 与 storage 值也都是 `pretty` → **保留 Pretty**
- **自查抓到一个我引入的泄漏**：viewer 页每次 Format 都调 mountViewer，菜单往 document 挂永不移除的 click 监听器 → 实测 5 次 render 累积 5 个（反复粘贴调试正是 viewer 页主场景）。已修 + 入网
- **审查拦截 6 条**（独立选靶，换肤半边 **7/7 存活**）：
  1. **回归**：`addMenuItem` 无 `visible:false` 默认 → Raw/Min 下 Collapse all 是死键，点了还把标签翻成"Expand all"对着全展开的树撒谎。**我自己的注释正好承诺了相反的事**
  2. **信任回归**：Sort 的唯一指示器被收进关着的菜单，而 `jk:sort` 跨会话持久化 → 排序一次后每个 JSON 都被重排，`Copy JSON` 静默给出重排结果、界面零提示。与"不偷改你的数据"直接冲突
  3. **L-009 第四次重演**：换肤是唯一从 innerHTML（幻影）搬进 createElement（可测）的控件，免死金牌到期却零测试
  4. **L-011 复发**：两条测试标题撒谎（"有且仅有五项"实断言四项；"onClick 会跑"从没点击过）
  5. `role="menu"`/`menuitem` 是空头支票，且 `<select>` 当 menu 子节点是**结构违规** → 摘掉
  6. `escAttr(v)` 是 cargo cult —— 转了属性位的 `v` 却没转文本位的 `t`，**不一致本身就证明是抄的**
- **裁决（第 2 条，产品级）**：保留 sort 功能与持久化，但把状态提到**常驻区**（`⇅ A→Z` 徽章 + tooltip 明说"Copy 给的是这个顺序"）。理由：藏在关着的菜单后面等于静默改数据
- **修复后**：195 测试；审查的 7 靶 6/7 被抓，第 7 个（tooltip 丢失）**按 L-008 不补测试** —— 视觉细节归冒烟清单，为它写断言是形式主义
- 冒烟清单新增第 20–22 项（弹窗内 stopPropagation 承重但桩不冒泡、Raw 下无 fold、排序徽章可见）

### T-005 详情

- **任务前提大部分不成立，如实记录**：设计稿是 Material Design 3 token 体系（Stitch 默认产物），而 design.md 明令"沿用现有四层机制不改"、档位又是结构基准 → 不该搬 MD3。且现有 CSS 一查：**零缺失样式、零死样式、无外链、四层机制完整**。"把设计稿翻译成 CSS"在 feature 1 范围内基本没有内容
- **但查出两个真 bug（都属 token 层）**：
  1. popup 与 viewer 各有一套 token 且**已漂移**（深色 5 值中 4 个不符：底 `#16181c` vs `#141619`、强调 `#4c8dff` vs `#5b9bff`）—— 对外宣称"统一设计语言"，实际两套
  2. popup **完全不跟随手动主题**（`jk:theme` 0 处引用）→ 深色 viewer + 浅色 popup
- **产出**：`tokens.css`(44) 单一来源；`viewer.css` 159→124 只剩组件；popup 改用共享 token + 读 `jk:theme`
- **我自己抓到的错误（L-012）**：断言"装载顺序错→界面无色"→ 变异顺序 → 红 → 宣布抓到。**理由是错的** —— CSS 变量在计算值阶段解析，`var()` 看得到后加载的定义，viewer.css 又定义 0 个 token，颠倒顺序什么也不会发生。**"测试断言 X → 变异 X → 它红"是纯循环论证**。已改为守"tokens.css 漏登记"（真会无色）+ "只有一个表定义 token"（守住顺序不重要的前提）
- **审查拦截 4 条**（独立选靶 13 个，**5 个存活**）：
  1. **严重**：**我自己写的注释毒化了自己的正则** —— tokens.css 顶部四层说明里有字面量 `:root[data-jk-theme="dark"]`，正则从注释开始匹配、`[^{]*` 穿过去抓到浅色块 → "深浅两套一致"变成拿浅色块和自己比。**删掉整个第 3 层实体块（☾ 强制深色彻底失效，正是本次要修的 bug）→ 223 全绿**。L-011 + L-007 合体
  2. 第 2 层（`@media` 系统深色）完全没网 —— 而第 2、3 层是逐字复制的两份，**整个改动的立意就是"不该有两份"，那两份之间却没有一致性断言**。下一次漂移的温床就在新文件里
  3. **popup.js 的注释在说谎**：`chrome.storage` 无同步读，回调必然落在首屏绘制之后 —— script 在 body 末尾只决定何时**运行**，对回调时机毫无影响。**FOUC 真实存在，而注释声称已避免**
  4. `assert.match(popupJs, /jk:theme/)` 是子串匹配 → 键名打成 `jk:themex` 照样绿、bug 静默复活
- **修复后**：225 测试；先剥注释再匹配 + 行首锚点；三块一致性（浅/系统深/手动深，名与值）全锁；键名改为两模块各自抠出比对；FOUC 注释**改成实话**（不做"隐藏 html 到回调"——那把一帧闪烁换成 storage 出问题时的空白 popup，而"popup 什么都没有"正是这个产品要解决的头号差评）

### T-007 详情

- **任务前提又是大部分不成立**：328px 已硬写、粘贴区结构完整、⌘/Ctrl+Enter 已在。真正的内容全是查出来的
- **查出两件**：
  1. **viewer 页也不跟手动主题**（T-005 只修了 popup）—— `applyTheme` 只在 `toolbar.mount` 里跑，而 mount 在用户点 Format **之后**，所以强制深色的用户打开 viewer 页，粘贴框和整页是白的。抽成共享的 `theme-boot.js`（**不各写一份** —— 那正是 popup/viewer 当初 token 漂移的成因），popup.js 里 T-005 加的那份删掉
  2. **发布包会把内部资料发给每个用户，是我造成的**：`CLAUDE.md` 记的打包命令是**排除式**（`zip -r . -x …`），打的是"没被明确排除的一切"。我一路加 `tests/` 和 `specs/` → 实跑 **74 文件 / 304K**，含 PRD、设计稿 PNG、LESSONS、METRICS、运行日志、全部测试代码。**扩展包人人可解压**。改白名单式 `pack.sh`（清单从 manifest 推导 + 禁入校验）→ **21 文件 / 60K**
- **审查拦截 3 条**：
  1. **回归（我亲手引入）**：`toolbar.js` 有一句**同步** `renderTheme()`，此时 `theme` 还是 `"auto"` → `applyTheme(_, "auto")` = `removeAttribute("data-jk-theme")` **打在 documentElement 上，正是 theme-boot 刚写的节点**。后果：深 →（点 Format）→ **白** → 深。**在 T-007 唯一要服务的那批用户身上平添一次白闪**。且 279 条测试**一条都没碰过 documentElement**
  2. **网的洞**：我给**键名**写了防线，却漏了同一契约的另一半 —— **storage area**。`local` 改 `sync` → 279 全绿 → 修复静默死掉（`"storage"` 权限同时涵盖两个 area，不报错不抛异常）
  3. **L-012 原样重演**：「没有会撑破它的 min-width」是**恒真断言** —— popup 的 `<style>` 里 min-width 出现 **0 次**，循环体从没执行过；且断言词里那个因果我从未验证（body 宽度写死，子元素 min-width 只会自己溢出）。**删除**，不是修
- **修复后**：282 测试；三个变异全抓（同步 renderTheme 回归 / area 改 sync / 键名打错）；冒烟新增第 24（发布前必跑打包并人眼过一遍）、25（两个扩展页面在强制主题下不发白）

### T-008 详情

- **产出**：三态齐全 —— 空输入禁用+tooltip / 非法就地报错（输入框描边 + 复用 jsonbig 报错） / 超长跳过试解析只给提示
- **超出任务书的两处判断**：
  - popup 引入 **util.js** 而不只 jsonbig（任务只写了后者）：没有 normalize，粘贴 XSSI/JSONP 会被判非法 —— **而那是产品宣传的容错能力**，且同一份内容进 viewer 却渲染正常，**popup 等于在骂自家 viewer 说谎**
  - 非法 JSON **不禁用**按钮：AC-004 说的是"输入时就地报错"（敲字本就不开标签页）；禁用会堵死修复路。审查核实此解读成立，但理由比我给的更实：**viewer 页今天就有粘贴框** —— 进去原文躺在可编辑区、错误在下方，改完重来。那不是废页，是**现在就通**的修复路
- **审查拦截 4 条**（独立选靶 13 个，9 抓 4 存活）：
  1. **回归（我造的）**：设计写"input 事件驱动 disabled"，只有试解析该防抖 —— 我把两者塞进同一个 300ms 定时器 → **粘贴后 300ms 内按钮禁用** → ⌘V→⌘+Enter 远快于 300ms → **"点了没反应"在旧代码根本没有的窗口里复活**，而那正是本任务要消灭的头号差评
  2. AC-004 第二半**完全没网**，且我的注释（"instead of after a wasted tab"）与放行的代码**自相矛盾**
  3. **桩不遵守 `disabled`** → 真浏览器对禁用按钮不派发 click → "按钮不该禁用时禁用了"这类 bug **结构上抓不到**，第 1 条正落在这个盲区（L-007）
  4. `.say { min-height:15px }` 注释称"按钮不会跳"，而大文件文案 67 字符必折 2 行 → **按钮仍下跳 ~15px，注释是假的**
- **审查自证的两条**：`LARGE` 双份（我审查期间自己修的，提进 util —— 那是**跨模块承诺**：popup 说"以 raw 打开"，core 决定是否真的以 raw 打开，漂了 popup 就在撒谎）；"打字卡顿"是伪担忧（1MB 实测 25ms，防抖 300ms 吸收得下）
- **修复后**：318 测试；桩加 `disabled` 支持后回归变异被抓（红 3）；文案缩到 26 字符（不折行，min-height 的承诺才成真）
- **L-014（新）**：状态行改 `textContent` 而非留 esc —— **能消除攻击面就别防御它**（防御要靠人记住它为什么在，消除不用）

### T-009 详情

- **产出**：空容器就地说 `empty object`/`empty array`（不再是 `0 keys`，与 `8 keys` 同一个待遇）；搜索零命中改为**树顶结果条 + 计数 `0/0`**，不再把全部 7/7 行降到 26% 不透明度（那与"查看器坏了"长得一样）
- **一处偏离设计**：空态说明**就地替换头行计数**，不另起"说明行"。我最初给的理由（"独立行会进 rows，搜 empty 会命中非数据行"）**被审查实测证伪** —— 就地替换照样命中 1 条非数据行（计数文本本来就在 rows 里，搜 `keys` 一直都能命中），只是把 2 减成 1。真实理由：**计数位置本来就在头行上，空态是同一信息的不同措辞，不该为它多造一行结构**。偏离本身成立（AC-005 过），但**理由必须是真的，否则下一个人会拿假前提做决策**
- **改动面溢出任务书**：任务只写 `tree.js`/`search.js`/`viewer.css`，实际必须动 **`core.js`** —— 结果条与 Raw 面板同住 `scrollEl`，切视图必须由 core 通知 search 撤条。已回填任务书
- **自查抓到的 3 件（审查跑动期间）**：
  1. **又一条恒真断言**：`jk-dim` 由 `classList.add` 添加，**不进 innerHTML** → `collectHTML().match(/jk-dim/)` **永远是 0**，那条"零命中不该变暗"绿是因为断言恒真（L-012 重演）。改走真实 `classList`
  2. **一个不存在的 bug**（→ L-015）：`.jk-nohits` 反复 mount 累积 4 条 —— 是**桩**清了 `children` 没清 `_q`，真实浏览器 `innerHTML=` 会销毁旧子树。没查就"修"会给 search.js 塞进一段永远绿的死代码
  3. **一个我造的真 bug**：搜无结果 → 切 Raw → `No match for "zzzz"` **挂在原文上方**。那条结论说的是树，Raw 没在显示树。修为 `onViewChange`，并补网（此前它一条测试都没红过）
- **审查拦截 4 条**（独立选靶 11 个，8 抓 **3 存活**；我自报"6/6 全抓"是 **L-009 第四次** —— 6 个靶全落在 `sayNoHits` 的调用路径上）：
  1. **`0/0` 根本没实现**：设计明写"顶部结果条 + 计数 `0/0`"，我留着 `findN.textContent = "0"` 一字未改 —— 而"工具栏一个光秃秃的 0"是我自己列的两个动机之一，**等于把要修的东西原样留下且没报告偏离**。真实场景：搜 `alpha`（`1/1`）→ 加一个字符变 `alphaz` → 结果条出现，**工具栏还写着 `1/1`**（正是 T-003 那类谎）
  2. **初始态无网**（→ L-016）：删掉 `noHits.hidden = true` **337 全绿** —— 假 storage 同步送达盖住了真实的异步窗口。真实后果：`hidden` 默认 false + padding/border/background = **每个 JSON 页一打开树顶挂一条空白灰条**（T-007 主题闪白同款）。改用 `{async:true}` 后变异被抓
  3. **DOM 顺序无网且桩够不到**：真实 DOM 里 `scrollEl` 有 `[data-pretty]`/`[data-raw]` 两个孩子（来自 SHELL 的 innerHTML 串），桩里它没有孩子 → `insertBefore(…, firstChild=null)` **等同** `appendChild` → "条挂底部"的变异**必然存活**。**不留恒真断言**（L-010）：删掉那条测试，转**冒烟第 26 条**
  4. 偏离理由造假（见上）
- **审查核实无问题的 3 条**（我点名重审的）：`sticky` 生效（`.jk-nohits` 是 `.jk-scroll` 的直接子节点，`.jk-row` 的 `z-index:auto` 不建立层叠上下文，`.jk-menu-pop` z20 仍压得住）；多次 mount **不泄漏**；且确认我给桩补的 `_q.clear()` **是对的且必要**
- **修复后**：337 测试全绿；11 靶 **10 抓**（第 11 靶桩物理够不到 → 冒烟 26）
- **新教训**：L-015（桩的失真会**两头**骗人 —— 也会凭空造 bug）、L-016（同步的假 storage 盖住真实异步窗口）；L-009 补入 T-009 行与一条可操作判据：**先列模块对外可观察的属性（文本/顺序/可见性/时序），没有断言的轴 = 必然存活的靶**
