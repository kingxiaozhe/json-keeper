{
  "name": "json-keeper",
  "version": "0.19.0",
  "private": true,
  "description": "Trustworthy JSON viewer Chrome extension — exact big integers, correctness diagnostics, no ads/telemetry.",
  "license": "MIT",
  "scripts": {
    "test": "node test/jsonbig.test.js && node test/core.test.js && node test/tree.test.js && node test/highlight.test.js && node test/viewer.test.js"
  }
}
