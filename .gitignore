# Never commit the extension signing key.
*.pem

# Dev / marketing artifacts not needed in the repo
design/
node_modules/
.DS_Store
*.log

# Per-machine Claude Code permission grants (the shared config is .claude/CLAUDE.md + rules/).
.claude/settings.local.json
